@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find swift-atomics if needed - this is a build dependency only
# The consuming project needs to handle this dependency
if(NOT TARGET Atomics)
    include(FetchContent)
    FetchContent_Declare(
        swift-atomics
        GIT_REPOSITORY https://github.com/apple/swift-atomics.git
        GIT_TAG        1.3.0
    )
    # Don't populate automatically, let the consuming project handle it
    FetchContent_GetProperties(swift-atomics)
    if(NOT swift-atomics_POPULATED)
        message(STATUS "SimplyCoreAudio requires swift-atomics. Please ensure it's available in your project.")
        FetchContent_MakeAvailable(swift-atomics)
    endif()
endif()

# Find required system libraries on Apple platforms
if(APPLE)
    find_library(COREAUDIO_FRAMEWORK CoreAudio REQUIRED)
    find_library(AUDIOTOOLBOX_FRAMEWORK AudioToolbox REQUIRED)
    find_library(FOUNDATION_FRAMEWORK Foundation REQUIRED)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/SimplyCoreAudioTargets.cmake")

check_required_components(SimplyCoreAudio)
